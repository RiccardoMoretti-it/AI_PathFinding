package Model.Entities;
import Model.Environment;

public abstract class Powerup extends Entity{

     protected Powerup(float[] position, Environment environment) {
          super(position, environment);
     }
     
}
